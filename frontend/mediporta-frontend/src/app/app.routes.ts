import { Routes } from '@angular/router';
import { TagsTableComponent } from './app/components/tags-table/tags-table.component';

export const routes: Routes = [
  { path: '', component: TagsTableComponent }
];
